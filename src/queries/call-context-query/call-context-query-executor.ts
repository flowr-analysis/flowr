import type { DataflowGraph } from '../../dataflow/graph/graph';
import type { CallContextQueryFormat } from './call-context-query-format';

export function executeCallContextQuery(graph: DataflowGraph, query: CallContextQueryFormat): void {

}
