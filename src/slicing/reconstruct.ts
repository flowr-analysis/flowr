import { DecoratedAst, NodeId } from '../r-bridge'

/**
 * Reconstructs parts of a normalized R ast into R code on an expression basis.
 */
export function reconstructToCode<Info>(ast: DecoratedAst<Info>, selection: NodeId[]): string {

  return ''
}
