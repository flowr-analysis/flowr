import { withShell } from '../helper/shell'

describe('Quad generation', withShell(shell => {
  const compareQuads = (code: string, expected: string) => {

  }

  it('should generate quads', () => {

  })
}))
